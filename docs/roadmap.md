# 路线与 TODO

## 路线总览

这份路线图按“先能聊，再能协作，再能执行，再能扩展”的顺序推进。

## 版本 0.1：MVP 核心交互

目标：让单聊和群聊先真正跑起来。

TODO：

- 建立 Electron + Vite + React 桌面壳
- 落地原型对应的 5 个页面路由
- 实现左侧导航、顶部栏、通知入口、资料入口
- 实现单聊会话列表和聊天线程
- 实现群聊会话列表和聊天线程
- 实现 `/skills`、`/command`、`/mcp` 的输入解析
- 实现任务 `pause`、`resume`、`cancel` 状态机
- 实现消息流中的执行中、已暂停、已完成、已取消状态
- 用本地 mock 数据先跑通全流程

验收：

- 用户能切换单聊和群聊
- 用户能发起一个任务并暂停或继续
- 用户能看到 Agent 的上下文回复和状态变化

## 版本 0.2：群组协作增强

目标：让群组不是简单多人列表，而是有上下文的协作空间。

TODO：

- 实现 manager agent 与 specialist agent 的角色划分
- 让群组共享上下文可见
- 让 Agent 之间可以互相 `@`
- 支持 Agent 之间的显式通信记录
- 支持群组内的任务拆分和回合式协作
- 支持群组任务的状态汇总

验收：

- 群组对话中能看到多个 Agent 参与
- Agent 能基于群组上下文回答
- Agent 之间的协作过程可追踪

## 版本 0.3：本地执行与扩展

目标：把“能聊”变成“能做事”。

TODO：

- 接入文件读取与写入工具
- 接入终端和 shell 执行工具
- 接入本地搜索工具
- 接入 `SKILL.md` 加载
- 接入 stdio MCP
- 接入 HTTP MCP
- 建立 provider registry
- 接入 OpenAI
- 接入 Qwen

验收：

- Agent 可以调用本地工具完成实际任务
- Agent 可以使用 Skills 和 MCP 扩展能力
- provider 切换可配置

## 版本 0.4：数据持久化与审计

目标：让本地状态可保存、可回看、可导出。

TODO：

- 建立 SQLite + Drizzle 数据层
- 持久化 conversations、messages、runs、artifacts
- 持久化 settings、profile、providers、mcp
- 保存会话 JSONL transcript
- 保存运行日志和审计日志
- 增加全文检索能力

验收：

- 重启应用后会话仍可恢复
- 任务状态和历史记录可查询
- 本地数据结构清晰可审计

## 版本 0.5：产品体验完善

目标：把原型中已经存在的完整产品面补齐。

TODO：

- 完善 Agent 管理页
- 完善群组管理页
- 完善扩展中心
- 完善仪表盘统计
- 完善设置页中的语言、主题、通知、模型配置
- 完善个人资料弹窗
- 完善通知中心
- 打磨视觉层级和状态反馈

验收：

- 产品结构和原型保持一致
- 关键操作的视觉反馈清晰
- 用户能感受到完整的桌面产品形态

## 后续版本候选

### 版本 0.6

- 更强的 run 可视化
- artifact 浏览器
- 更细的任务历史视图

### 版本 0.7

- 更丰富的群组编排
- 任务模板
- 团队角色预设

### 版本 0.8

- 更完整的插件权限模型
- Skills 市场化管理
- MCP 服务可视化配置

